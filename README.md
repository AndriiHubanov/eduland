# EDULAND 🏙️

Освітня стратегічна гра для українського коледжу.

## Налаштування

### 1. Встановіть залежності
```bash
npm install
```

### 2. Налаштуйте Firebase

1. Перейдіть на [Firebase Console](https://console.firebase.google.com/)
2. Створіть проєкт (або використайте існуючий)
3. Увімкніть **Firestore Database** (в режимі test mode для початку)
4. Увімкніть **Storage**
5. Скопіюйте конфіг з Project Settings → Your apps

### 3. Налаштуйте змінні середовища

```bash
cp .env.example .env
```

Відкрийте `.env` і вставте ваші Firebase ключі.

### 4. Правила Firestore

У Firebase Console → Firestore → Rules вставте:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

> ⚠️ Це тимчасові правила для розробки. Перед продакшном налаштуйте безпечні правила.

### 5. Запустіть проєкт

```bash
npm run dev
```

Відкрийте http://localhost:5173

## Структура

```
src/
├── pages/        # Сторінки гри
├── components/   # Компоненти
├── firebase/     # Firebase конфіг і сервіс
├── store/        # Zustand стейт
└── styles/       # CSS стилі
```

## Хостинг (Vercel)

```bash
npm run build
# Деплой через Vercel Dashboard або CLI
```

## Адмінка

Перейдіть на `/admin` · Пароль: `nova2047`
