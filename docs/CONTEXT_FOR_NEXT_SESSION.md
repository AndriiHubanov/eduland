# 🚀 Інструкція для наступної сесії Claude Code

> Вставити цей файл на початку нової сесії. Розробка починається одразу.

---

## Проєкт: Eduland

Браузерна освітня гра (React + Firebase) у пост-апокаліптичному сеттингу.
Студенти будують поселення, виконуючи навчальні завдання.

**Репозиторій:** `AndriiHubanov/eduland`
**Робочий каталог:** `/home/user/eduland`
**Деплой:** Firebase Hosting (GitHub Actions CI/CD)

---

## Поточний стан (після Фази 8)

Що вже реалізовано:
- ✅ Система замку (рівні 1–5), будівлі виробляють ресурси
- ✅ Армія: 8 класів юнітів, формація, апгрейди
- ✅ Карта світу з руїнами (tier 1–3), бойовий екран
- ✅ Місії (panel), система XP і рівнів
- ✅ Природничі науки: 4 ресурси (energy, bio, crystal, code), 4 будівлі
- ✅ Система завдань від викладача (SurveyPage)
- ✅ Firebase Realtime DB + Auth + Hosting
- ✅ GitHub Actions CI/CD

Що НЕ реалізовано (треба в фазах 9–15):
- ❌ 31 поле на карті (12 ресурс / 7 руїни / 12 нейтральних), 48г рефреш
- ❌ Лабораторії для дослідження полів (як MyLands)
- ❌ Tech Tree для природничих наук
- ❌ Daily/Weekly місії (автогенерація)
- ❌ Адмінка для викладача (навички, завдання, поля гравців)
- ❌ UI на 1 екран (зараз розкидано по сторінках)

---

## Завдання — Фаза 9: Система полів

**Гілка:** `phase9` (від `main`)

### Що треба зробити:

#### 1. Firebase схема полів
Додати в `/src/config/fields.js`:
```js
export const FIELDS_CONFIG = {
  total: 31,
  resourceFields: 12,   // видобуток ресурсів
  ruinFields: 7,        // руїни для штурму
  neutralFields: 12,    // нейтральні
  refreshHours: 48,     // кожні 48 годин
}
```

Firebase структура (додати в `database.rules.json` та `src/services/fieldService.js`):
```
/fields/{fieldId}
  type: "resource" | "ruin" | "neutral"
  tier: 1 | 2 | 3
  resourceType: "energy"|"bio"|"crystal"|"code"|null
  ruinHP: number | null
  resetAt: timestamp
  lastOccupiedBy: userId | null
  extractionBonus: number (0-50)

/fieldEvents/{eventId}
  fieldId, userId, action, timestamp, result
```

#### 2. Ініціалізація полів
Скрипт або Firebase function що:
- Створює 31 поле при першому запуску
- Кожні 48г скидає resetAt → реалізувати через client-side check

#### 3. WorldMap Component
Замінити поточну карту на сітку 31 поля:
- Компактні тайли (7×5 або hex)
- Колір по типу: зелений/червоний/сірий
- На кожному тайлі: іконка типу + таймер до рефрешу
- Клік → модалка з деталями поля + кнопки дій
- Фільтри: "Всі" / "Доступні" / "Мої" / "Руїни"

#### 4. Стан полів у React
- `useFields()` hook → підписка на `/fields` в Firebase
- `useFieldTimer()` → countdown до 48г рефрешу

### Критерій готовності Фази 9:
- [ ] 31 поле ініціалізоване в Firebase (скрипт або seed)
- [ ] WorldMap показує всі 31 поле з типами і таймерами
- [ ] Тайл рефрешується в UI коли `resetAt` минув
- [ ] Можна клікнути поле → бачити деталі
- [ ] PR `phase9 → main` готовий

---

## Наступні фази (коротко)

| Фаза | Гілка | Суть |
|------|-------|------|
| 10 | `phase10` | Лабораторії: відправка команди в поле, видобуток, штурм руїн |
| 11 | `phase11` | Daily/weekly місії + Tech Tree (природничі науки) |
| 12 | `phase12` | Картинки (промпти в PHASE8_IMAGE_PROMPTS.md) + UI 1 екран |
| 13 | `phase13` | Адмінка: гравці, їх навички, завдання, поля |
| 14 | `phase14` | Баланс, перформанс, PWA, onboarding |
| 15 | `phase15` | Запуск Season 1 |

Детально: `docs/ROADMAP_PHASES_9_15.md`

---

## Технічний стек

```
Frontend: React (CRA або Vite), CSS Modules
Backend: Firebase Realtime Database, Firebase Auth
Деплой: Firebase Hosting via GitHub Actions
Стан: React Context + custom hooks
```

## Ключові файли

```
src/
  components/
    WorldMap/        ← тут буде нова система полів
    City/            ← сучасне місто (вже переписано)
    Admin/           ← буде адмінка (фаза 13)
  services/
    fieldService.js  ← створити для фази 9
    ruinService.js   ← вже є, треба інтегрувати з полями
  hooks/
    useFields.js     ← створити для фази 9
  config/
    fields.js        ← конфіг полів
```

---

## Git-команди для старту фази 9

```bash
git checkout main
git pull origin main
git checkout -b phase9

# ... розробка ...

git add -A
git commit -m "feat(phase9): 31-field system with 48h refresh + WorldMap grid"
git push -u origin phase9

# Після PR: наступна фаза
git checkout main && git pull origin main
git checkout -b phase10
```

---

## Важливо

- **Лише природничі науки** (bio, chemistry, physics, geography) для Season 1
- **UI max 2 сторінки**: головна (місто + карта) + адмінка
- **Після кожної фази** — окремий PR
- **Не чіпати** `main` напряму — тільки через PR
